[{"content":"import { render, screen, within } from '@testing-library/react';\n\nconst FormData = () => {\n  return (\n    <div>\n      <button>go back</button>\n      <form aria-label=\"form\">\n        <button>save</button>\n        <button>cancel</button>\n      </form>\n    </div>\n  );\n};\n\nrender(<FormData />);","type":"code","id":"zpq8c"},{"content":"const toContainRole = (container, role, quantity = 1) => {\r\n  const elements = within(container).queryAllByRole(role);\r\n\r\n  if (elements.length === quantity) {\r\n    return {\r\n      pass: true,\r\n    };\r\n  }\r\n  return {\r\n    pass: false,\r\n    message: () =>\r\n      `expected ${container} to have ${quantity} items with role ${role}. Found ${elements.length} instead.`,\r\n  };\r\n};\r\nexpect.extend({ toContainRole });","type":"code","id":"47mov"},{"content":"test('the form displays two buttons', async () => {\n  render(<FormData />);\n\n  const form = screen.getByRole('form');\n \n  expect(form).toContainRole('button', 2)\n});","type":"code","id":"ulbfk"}]